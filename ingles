#!/bin/bash

# Arquivo onde todas as frases serão armazenadas
ARQUIVO="$HOME/Documentos/Ingles/anotacoes.txt"

# Criar diretório se não existir
mkdir -p "$(dirname "$ARQUIVO")"
touch "$ARQUIVO"

# Cores
BRANCO="\e[97m"
AZUL="\e[34m"
VERDE="\e[32m"
VERMELHO="\e[31m"
RESET="\e[0m"

# Adicionar frases ao arquivo (evitando duplicatas)
adicionar_frases() {
    echo -e "${AZUL}Digite frases de inglês (inclua a pronúncia entre () e a tradução entre [])."
    echo -e "Pressione Enter sem digitar nada para sair.${RESET}"

    while true; do
        read -p "Frase: " frase
        [[ -z "$frase" ]] && break  # Sai do loop se a entrada estiver vazia
        
        # Verifica se a frase já existe no arquivo
        if grep -Fxq "$frase" "$ARQUIVO"; then
            echo -e "${VERMELHO}⚠️ Frase já cadastrada!${RESET}"
        else
            echo "$frase" >> "$ARQUIVO"
            echo -e "${VERDE}✅ Frase salva!${RESET}"
        fi
    done
}

# Exibir todas as frases salvas
exibir_frases() {
    if [[ -s "$ARQUIVO" ]]; then
        echo -e "\n📖 ${BRANCO}Frases salvas:${RESET}\n"
        while IFS= read -r linha; do
            frase=$(echo "$linha" | sed -E 's/\(.*\)//' | sed -E 's/\[.*\]//')
            pronuncia=$(echo "$linha" | grep -oP '\(.*?\)')
            traducao=$(echo "$linha" | grep -oP '\[.*?\]')

            echo -e "${BRANCO}Frase: $frase${RESET}"
            [[ -n "$pronuncia" ]] && echo -e "${AZUL}Pronúncia: $pronuncia${RESET}"
            [[ -n "$traducao" ]] && echo -e "${VERDE}Tradução: $traducao${RESET}"
            echo -e "----------------------------------"  # Separador
        done < "$ARQUIVO"
    else
        echo -e "${VERMELHO}Nenhuma frase adicionada ainda.${RESET}"
    fi
}

# Pesquisar frases por palavra-chave
pesquisar_frases() {
    echo -e "${AZUL}Digite o termo de busca:${RESET}"
    read termo

    resultado=$(grep -i --color=never "$termo" "$ARQUIVO" 2>/dev/null)

    if [[ -z "$resultado" ]]; then
        echo -e "${VERMELHO}Nenhuma frase encontrada.${RESET}"
    else
        echo -e "\n🔎 ${BRANCO}Resultados encontrados:$(echo "$resultado" | wc -l)${RESET}\n"
        echo "$resultado" | while IFS= read -r linha; do
            frase=$(echo "$linha" | sed -E 's/\(.*\)//' | sed -E 's/\[.*\]//')
            pronuncia=$(echo "$linha" | grep -oP '\(.*?\)')
            traducao=$(echo "$linha" | grep -oP '\[.*?\]')

            echo -e "${BRANCO}Frase: $frase${RESET}"
            [[ -n "$pronuncia" ]] && echo -e "${AZUL}Pronúncia: $pronuncia${RESET}"
            [[ -n "$traducao" ]] && echo -e "${VERDE}Tradução: $traducao${RESET}"
            echo -e "----------------------------------"
        done
    fi
}

# Menu principal
while true; do
    echo -e "\n📚 ${BRANCO}Diário de Inglês - Anotações Ilimitadas${RESET}\n"
    echo -e "1) ✏️  Adicionar frases"
    echo -e "2) 📖 Ver todas as frases"
    echo -e "3) 🔍 Pesquisar frases"
    echo -e "4) ❌ Sair"
    read -p "Escolha uma opção: " opcao

    case $opcao in
        1) adicionar_frases ;;
        2) exibir_frases ;;
        3) pesquisar_frases ;;
        4) echo -e "${VERMELHO}Saindo...${RESET}"; exit 0 ;;
        *) echo -e "${VERMELHO}Opção inválida!${RESET}" ;;
    esac
done
